import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen bg-gray-200">
      <div className="text-4xl font-semibold text-gray-800">Loading...</div>
    </div>
  );
};

export default Loading;
